name: Python CI

on:
  push:
    branches:
      - feature/quiz4  # Adjust to match your branch name

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt  # Add this line if you have a requirements.txt file
        pip install pytest  # Add this line to install pytest

    - name: Run tests
      run: |
<<<<<<< HEAD
        python -m pytest tests/test_app.py
=======
      python -m pytest tests/test_app.py
        

    

>>>>>>> refs/remotes/origin/feature/quiz4
